package cmd

func getEpisodeByFilename(filename string) /* ??? */ {
	// Take a video filename, parse out the name of the show and episode number.
	// Identify with TVDB
	// Return episode struct
}

func getMovieByFilename(filename string) /* ??? */ {
	// Take video filename, parse out the name (and optionally year?) of the movie
	// Identify with TVDB
	// Return movie struct
}
